/*******************************************************************************
        => STM32 文档
*******************************************************************************/
        rw      // 读/写 (rw) 软件可以读写这些位
        r       // 只读  (r)  软件只能读取这些位。
        w       // 只写  (w)  软件只能写入该位。 读取该位时将返回复位值。
        rc_w1   // 读取 /清零 (rc_w1) 软件可以读取该位,也可以通过写入 1 将该位清零。
                // 写入“0”对该位的值无影响。
        rc_w0   // 读取 /清零 (rc_w0) 软件可以读取该位,也可以通过写入 0 将该位清零。
                // 写入“1”对该位的值无影响。
        rc_r    // 读取 /读取清零(rc_r) 软件可以读取该位。读取该位时,将自动清零。
                // 写入“0”对该位的值无影响。
        rs      // 读取 /置位 (rs) 软件可以读取该位,也可将其置 1。
                // 写入“0”对该位的值无影响。
        rt_w    // 只读写触发 (rt_w) 软件可以读取该位。写入“0”或“1”时,
                // 将触发事件,但不会影响该位的值。
        t       // 切换 (t) 软件只能通过写入“1”来切换该位。写入“0”无影响。






/*******************************************************************************
        => 
*******************************************************************************/
        provide                 // 提供
        independent             // 独立的
        crystal oscillator      // 晶体控制振荡器
        Ring Oscillator         // 环形振荡器
