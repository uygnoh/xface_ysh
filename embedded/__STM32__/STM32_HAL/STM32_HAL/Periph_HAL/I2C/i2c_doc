/*******************************************************************************
        => STM32F429__IIC__
*******************************************************************************/
I2C_SDA         //设置为（开漏输出），即可以输出，也可以输入
                //也可以设置为（推挽输出），但是你输入的时候需要设置为（输入模式）
I2C_SCK         //设置为（开漏输出）
